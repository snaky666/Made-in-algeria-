Nova Web - Backend

1. Install dependencies: npm install
- Set environment variables from .env.example
2. Create the Supabase table (see deploy SQL in root)
3. Start: npm start
4. Use POST /api/leads to insert test leads. Start worker with POST /api/start-worker
